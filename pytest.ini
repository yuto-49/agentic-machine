[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
markers =
    unit: Unit tests (no external dependencies)
    integration: Integration tests (in-memory DB, test client)
    e2e: End-to-end tests (requires ANTHROPIC_API_KEY)
    slow: Slow tests (>5s)
testpaths = tests
